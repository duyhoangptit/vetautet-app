package com.vetautet.app.application.user.dto;

/**
 * Which user attribute an availability check targets.
 * Application layer - use case DTO.
 */
public enum AvailabilityCheckType {
    USERNAME,
    EMAIL
}
